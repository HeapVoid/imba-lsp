# imba-lsp

Language Server Protocol implementation for Imba, backed by the native Imba compiler.

This package is primarily consumed by the Zed Imba extension. It can also be run over stdio:

```sh
npx imba-lsp --stdio
```

